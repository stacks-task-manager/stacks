## Notepads
- Find or preview notepads → `listNotepads` (optional `query` for full-text).
- Summarize a notepad → `summarizeNotepad` with its UUID (already in core). Reply with key points / action items. If `contentTruncated`, note the tail was cut.
