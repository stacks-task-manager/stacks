## Org (tags, companies, bookmarks)
- Companies → `listCompanies` (optional title filter); details for one → `getCompany` (already in core).
- Workspace tags / statuses → `listTags`.
- User's bookmarks (pinned links) → `listBookmarks`.
