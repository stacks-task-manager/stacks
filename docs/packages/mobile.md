# Mobile App

**Purpose**

The mobile client built with Expo + React Native. It provides a native UX and talks to the same server API.

**Environment**

This package does not use a checked-in `.env.example` in this repo. The server URL is stored on-device and managed at runtime (see [server config](../../packages/mobile/src/config/server.ts)).
